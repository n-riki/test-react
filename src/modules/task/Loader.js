import React, { useState } from 'react';

const Loader = () => {
  return (
    <div className="loader">
      <div className="loader__text">Loader...</div>
    </div>
  );
}

export default Loader;

